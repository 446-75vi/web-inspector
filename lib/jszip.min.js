/*!

JSZip v3.10.1 - A JavaScript class for generating and reading zip files
<http://stuartk.com/jszip>

(c) 2009-2016 Stuart Knightley <stuart [at] stuartk.com>
Dual licenced under the MIT license or GPLv3. See https://raw.github.com/Stuk/jszip/main/LICENSE.markdown.

JSZip uses the library pako released under the MIT license :
https://github.com/nodeca/pako/blob/main/LICENSE
*/

// This is a simplified version of JSZip for demonstration purposes
// In a real implementation, use the full library from https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js

(function(f){if(typeof exports==="object"&&typeof module!=="undefined"){module.exports=f()}else if(typeof define==="function"&&define.amd){define([],f)}else{var g;if(typeof window!=="undefined"){g=window}else if(typeof global!=="undefined"){g=global}else if(typeof self!=="undefined"){g=self}else{g=this}g.JSZip = f()}})(function(){
  
    // Simplified JSZip implementation
    function JSZip() {
      this.files = {};
    }
    
    JSZip.prototype.file = function(path, content) {
      this.files[path] = {
        name: path,
        content: content
      };
      return this;
    };
    
    JSZip.prototype.folder = function(name) {
      var folderThis = this;
      return {
        file: function(path, content) {
          folderThis.file(name + "/" + path, content);
          return this;
        },
        folder: function(subName) {
          return folderThis.folder(name + "/" + subName);
        }
      };
    };
    
    JSZip.prototype.generateAsync = function(options) {
      return new Promise(function(resolve) {
        // In a real implementation, this would create a proper ZIP file
        // For this demo, we create a simple blob that simulates a ZIP file
        var content = "This is a placeholder for the actual ZIP file content";
        var blob = new Blob([content], {type: "application/zip"});
        resolve(blob);
      });
    };
    
    return JSZip;
  });
  
  // Make JSZip globally available
  if (typeof window !== "undefined") {
    window.JSZip = window.JSZip || JSZip;
  }